query singleUser($id: String!) {
  user(id: $id) {
    lastname
    firstName
    email
    nickname
  }
}

query CurrentUser {
  currentUser {
    lastname
    firstName
    email
    nickname
  }
}

query CurrentUserId {
  currentUser {
    id
  }
}
